export enum TournamentStatus {
  pending = 'pending',
  created = 'created',
  finished = 'finished',
  ongoing = 'ongoing',
}

export enum TournamentType {
  Master = 'master',
  League = 'league',
}

export enum TournamentSeason {
  Apertura = 'apertura',
  Clausura = 'clausura',
  Master = 'master',
  Anual = 'anual',
}
