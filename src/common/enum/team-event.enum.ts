export enum TeamEventStatus {
  draft = "draft",
  registration = "registration",
  active = "active",
  finished = "finished",
}

export enum TeamEventSeriesPhase {
  regular = "regular",
  final = "final",
}

export enum TeamEventSeriesStatus {
  pending = "pending",
  inProgress = "in_progress",
  completed = "completed",
  walkover = "walkover",
}

export enum TeamEventMatchType {
  singles1 = "singles_1",
  singles2 = "singles_2",
  doubles = "doubles",
}

export enum TeamEventMatchStatus {
  pending = "pending",
  played = "played",
  walkover = "walkover",
}

export enum TeamEventMatchSide {
  home = "home",
  away = "away",
}
