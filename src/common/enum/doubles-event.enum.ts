export enum DoublesEventStatus {
  draft = 'draft',
  active = 'active',
  finished = 'finished',
}

export enum DoublesMatchStatus {
  pending = 'pending',
  played = 'played',
  cancelled = 'cancelled',
}

export enum DoublesMatchPhase {
  zone = 'zone',
  playoff = 'playoff',
}
